workflows:
  ios-capacitor-build:
    name: iOS Capacitor Build - Ghost Peek
    max_build_duration: 60
    instance_type: mac_mini_m2
    
    environment:
      xcode: latest
      node: 20
      cocoapods: default
      groups:
        - ios_credentials # Configure in Codemagic UI
      vars:
        BUNDLE_ID: "app.plantain7502.soybean5714"
        APP_NAME: "Ghost Peek"
    
    cache:
      cache_paths:
        - $HOME/.npm
        - $HOME/Library/Caches/CocoaPods
        - $CM_BUILD_DIR/capacitor-app/node_modules
        - $CM_BUILD_DIR/capacitor-app/ios/Pods
    
    scripts:
      # Entrar na pasta do app Capacitor
      - name: Install dependencies
        script: |
          cd capacitor-app
          npm install
      
      # Build do projeto Vite
      - name: Build web app
        script: |
          cd capacitor-app
          npm run build
      
      # Adicionar e sincronizar iOS
      - name: Add and sync iOS platform
        script: |
          cd capacitor-app
          npx cap add ios || true  # Ignora se já existe
          npx cap sync ios
      
      # Instalar CocoaPods
      - name: Install CocoaPods dependencies
        script: |
          cd capacitor-app/ios/App
          pod install
      
      # Configurar code signing (requer certificados no Codemagic)
      - name: Set up code signing
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_DEVELOPMENT
          keychain add-certificates
          xcode-project use-profiles --project capacitor-app/ios/App/App.xcodeproj
      
      # Build do projeto iOS
      - name: Build iOS app
        script: |
          cd capacitor-app/ios/App
          xcode-project build-ipa \
            --workspace App.xcworkspace \
            --scheme App \
            --config Release \
            --archive-flags="-destination 'generic/platform=iOS'"
    
    artifacts:
      - capacitor-app/ios/App/build/ios/ipa/*.ipa
      - capacitor-app/ios/App/build/ios/xcarchive/*.xcarchive
      - /tmp/xcodebuild_logs/*.log
    
    publishing:
      email:
        recipients:
          - your-email@example.com # Ajuste para seu email
        notify:
          success: true
          failure: true

  # Workflow alternativo para build sem code signing (simulador apenas)
  ios-simulator-build:
    name: iOS Simulator Build (No Signing)
    max_build_duration: 45
    instance_type: mac_mini_m2
    
    environment:
      xcode: latest
      node: 20
      cocoapods: default
      vars:
        BUNDLE_ID: "app.plantain7502.soybean5714"
        APP_NAME: "Ghost Peek"
    
    cache:
      cache_paths:
        - $HOME/.npm
        - $HOME/Library/Caches/CocoaPods
        - $CM_BUILD_DIR/capacitor-app/node_modules
        - $CM_BUILD_DIR/capacitor-app/ios/Pods
    
    scripts:
      - name: Install dependencies
        script: |
          cd capacitor-app
          npm install
      
      - name: Build web app
        script: |
          cd capacitor-app
          npm run build
      
      - name: Add and sync iOS platform
        script: |
          cd capacitor-app
          npx cap add ios || true
          npx cap sync ios
      
      - name: Install CocoaPods dependencies
        script: |
          cd capacitor-app/ios/App
          pod install
      
      - name: Build for Simulator
        script: |
          cd capacitor-app/ios/App
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 15 Pro' \
            -derivedDataPath build \
            build
    
    artifacts:
      - capacitor-app/ios/App/build/**/*.app
      - /tmp/xcodebuild_logs/*.log
    
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true
